@{
    ViewBag.Title = "错误";
}

<script>
    var errorMessage = "@Html.Raw(ViewData["ErrorMessage"])";
    if (errorMessage) {
        alert(errorMessage);
        window.history.back();
    }
</script>